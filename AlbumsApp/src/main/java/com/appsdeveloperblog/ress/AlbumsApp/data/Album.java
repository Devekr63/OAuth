package com.appsdeveloperblog.ress.AlbumsApp.data;

public record Album(
        String userId,
        String albumTitle,
        String albumId,
        String albumDescription,
        String albumUrl
){}
